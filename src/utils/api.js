const HOST = "http://localhost:5000";

const CATEGORY_API = "/api/category";
const EXPENSE_API = "/api/expense";

export const getAllCategories = `${HOST}${CATEGORY_API}/`;
export const addCategory = `${HOST}${CATEGORY_API}/`;
